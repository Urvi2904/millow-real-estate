/**
 * This file is automatically loaded by Create React App when running `npm test`.
 * It sets up additional testing utilities for your tests using Jest and Testing Library.
 *
 * `@testing-library/jest-dom` adds custom matchers to make tests more readable:
 * Example:
 *    expect(element).toHaveTextContent(/my text/i)
 *    expect(button).toBeDisabled()
 */
import '@testing-library/jest-dom';
